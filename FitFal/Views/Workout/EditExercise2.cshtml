@model FitFalMVC.Application.ViewModels.WorkoutVmDirector.NewWorkoutExerciseVm

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<h2>title</h2>


<div class="row">
    <div class="col-md-4">
        <form asp-action="EditExercise2">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <input type="hidden" name="ExerciseId" value="@Model.ExerciseId"/>
            <input type="hidden" name="WorkoutId" value="@Model.WorkoutId"/>
            <input type="hidden" name="Id" value="@Model.Id"/>

            <div class="form-group">
                <label asp-for="Sets" class="control-label">Sets</label>
                <input asp-for="Sets" class="form-control"/>
                <span asp-validation-for="Sets" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Reps" class="control-label">Reps</label>
                <input asp-for="Reps" class="form-control"/>
                <span asp-validation-for="Reps" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Weight" class="control-label">Cieżar</label>
                <input asp-for="Weight" class="form-control"/>
                <span asp-validation-for="Weight" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary"/>
            </div>
        </form>
    </div>

</div>